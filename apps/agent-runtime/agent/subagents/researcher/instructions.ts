import { resolveInstructions } from "../../lib/resolve-instructions";
import DEFAULT_INSTRUCTIONS from "./instructions.default";

export default resolveInstructions("researcher", DEFAULT_INSTRUCTIONS);
